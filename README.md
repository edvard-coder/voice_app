# Voice Manager

Voice Manager - это приложение для преобразования голоса в текст с возможностью автоматической вставки в активное окно. Пользователь нажимает кнопку микрофона (или горячую клавишу), программа слушает голос, распознаёт речь и вставляет текст туда, где сейчас курсор (в любое активное приложение: браузер, мессенджер, документ).

## Особенности

- Голосовой ввод в текст с мгновенной вставкой
- Поддержка русского и английского языков
- Офлайн распознавание (не требует интернета)
- Горячая клавиша: `Ctrl+Alt+Space` для начала/останова записи
- Автоматическая загрузка языковых моделей
- Простая установка и запуск

## Установка и запуск из исходников

1. Убедитесь, что у вас установлен Python 3.7+
2. Склонируйте репозиторий:
   ```bash
   git clone <your-repo-url>
   cd voice-manager
   ```
3. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```
4. Запустите приложение:
   ```bash
   python -m src.voice_manager.main
   ```

## Запуск из готового exe

1. Скачайте последнюю версию из [Releases](https://github.com/your-username/voice-manager/releases)
2. Распакуйте архив
3. Запустите `VoiceManager.exe`
4. При первом запуске приложение автоматически скачает языковые модели

## Использование

1. Выберите язык распознавания: русский, английский или авто
2. Включите/выключите автовставку в активное окно
3. Нажмите на кнопку микрофона 🎙️ или используйте горячую клавишу `Ctrl+Alt+Space`
4. Говорите, когда индикатор показывает "Запись..."
5. Нажмите кнопку стоп ⏹️ или снова горячую клавишу, чтобы остановить запись
6. Текст появится в поле предпросмотра и будет вставлен в активное окно (если включена автовставка)

## Сборка exe

Для сборки приложения в exe:

1. Установите PyInstaller:
   ```bash
   pip install pyinstaller
   ```
2. Выполните сборку:
   ```bash
   pyinstaller --onefile --windowed --add-data "models;models" --name "VoiceManager" src/voice_manager/main.py
   ```

## Структура проекта

```
voice-manager/
├── src/
│   └── voice_manager/
│       ├── __init__.py
│       ├── gui.py          # Графический интерфейс
│       ├── stt_vosk.py     # Распознавание речи
│       ├── hotkeys.py      # Обработка горячих клавиш
│       ├── paste.py        # Вставка текста
│       └── main.py         # Точка входа
├── scripts/                # Скрипты сборки
├── models/                 # Языковые модели (скачиваются автоматически)
├── requirements.txt        # Зависимости Python
├── .github/workflows/      # GitHub Actions workflows
├── web/                    # Веб-версия (опционально)
└── README.md
```

## Технологии

- **GUI**: tkinter
- **STT**: Vosk (офлайн распознавание)
- **Запись аудио**: PyAudio
- **Горячие клавиши**: keyboard + pynput
- **Вставка текста**: pyautogui
- **Сборка exe**: PyInstaller

## GitHub Actions

Репозиторий включает GitHub Actions workflows:
- При пуше в ветку main: сборка артефакта
- При теге v*: сборка и публикация в GitHub Releases

## Веб-версия

В папке `web/` также доступна веб-версия приложения с использованием Web Speech API, которая не требует установки Python или других зависимостей.